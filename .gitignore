.wrangler/
node_modules/

# Artefacts des tests Playwright
test-results/
playwright-report/

# Fichiers sensibles — ne pas déployer sur Cloudflare
supabase/
Code.gs
PROMPT.txt
DECISIONS.txt
DEPLOIEMENT.md

# Backups CSV/ZIP — contiennent des données personnelles (RGPD)
# Accessibles via GitHub Artifacts uniquement, jamais dans le repo
backups/
.claude/
