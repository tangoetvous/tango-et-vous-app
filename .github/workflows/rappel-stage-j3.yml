name: Stages — rappel J-3 (S4)

on:
  schedule:
    # 7h UTC = 9h Paris en été (CEST) · 8h Paris en hiver (CET)
    - cron: '0 7 * * *'
  workflow_dispatch:
    inputs:
      date:
        description: 'Date du stage YYYY-MM-DD (défaut : dans 3 jours)'
        required: false

jobs:
  send-rappel-stage-j3:
    runs-on: ubuntu-latest
    steps:
      - name: Envoyer emails S4 (rappel J-3 stages)
        run: |
          if [ -n "${{ github.event.inputs.date }}" ]; then
            TARGET_DATE="${{ github.event.inputs.date }}"
          else
            TARGET_DATE=$(date -d "+3 days" +%Y-%m-%d)
          fi
          echo "Date cible : $TARGET_DATE"
          RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
            -X POST "https://app.tangoetvous.fr/api/cron/rappel-stage-j3" \
            -H "Content-Type: application/json" \
            -H "X-Cron-Secret: ${{ secrets.CRON_SECRET }}" \
            -d "{\"date\":\"$TARGET_DATE\"}")
          HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
          BODY=$(echo "$RESPONSE" | grep -v "HTTP_STATUS:")
          echo "Réponse : $BODY"
          echo "Statut HTTP : $HTTP_STATUS"
          if [ "$HTTP_STATUS" != "200" ]; then
            echo "::error::Échec de l'envoi (HTTP $HTTP_STATUS)"
            exit 1
          fi
